{
  "scripts": {
    "test": "cd src/ && npm link",
    "build": "gulp",
    "push": "npm run build && cd dist/ && npm publish",
    "push:cnj": "nrm use cnj && npm run build && cd dist/ && npm publish",
    "push:npm": "nrm use npm && npm run build && cd dist/ && npm publish"
  },
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "del": "^7.0.0",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}
